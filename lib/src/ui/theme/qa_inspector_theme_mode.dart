/// The appearance used by QA Inspector independently of the host application.
enum QaInspectorThemeMode {
  /// The dark QA Inspector palette.
  dark,

  /// The light QA Inspector palette.
  light,
}
